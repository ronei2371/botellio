# ============================================
# CONFIGURAÇÕES DO BOTELLIO v1.1
# ============================================
# IMPORTANTE: Copie este arquivo para .env e preencha com suas chaves reais
# Nunca compartilhe suas chaves de API!

# ============================================
# 1. GROK (xAI) - OBRIGATÓRIO
# ============================================
# Obtenha sua chave em: https://console.x.ai/
# 1. Crie uma conta em x.ai
# 2. Acesse o console de desenvolvedores
# 3. Gere uma nova API key
# 4. Cole a chave abaixo

GROK_API_KEY=sua-chave-grok-aqui
GROK_MODEL=grok-beta

# ============================================
# 2. WHATSAPP BUSINESS API - OBRIGATÓRIO
# ============================================
# Obtenha suas credenciais em: https://developers.facebook.com/apps/
# 
# PASSO A PASSO:
# 1. Acesse https://developers.facebook.com/apps/
# 2. Crie um novo app ou selecione um existente
# 3. Adicione o produto "WhatsApp"
# 4. Vá em "WhatsApp" > "API Setup"
# 5. Copie os valores abaixo:

# Token de acesso temporário (válido por 24h - depois você precisará gerar um permanente)
WHATSAPP_API_TOKEN=seu-token-whatsapp-aqui

# ID do número de telefone (encontrado na seção "Phone Number ID")
WHATSAPP_PHONE_NUMBER_ID=seu-phone-id-aqui

# ID da conta comercial (encontrado na seção "WhatsApp Business Account ID")
WHATSAPP_BUSINESS_ACCOUNT_ID=seu-business-id-aqui

# Token de verificação (você cria este token - pode ser qualquer string segura)
# Exemplo: botellio_verify_2024_xyz123
VERIFY_TOKEN=seu-verify-token-personalizado-aqui

# ============================================
# 3. CONFIGURAÇÕES DA APLICAÇÃO
# ============================================
# Ambiente: development (desenvolvimento) ou production (produção)
FLASK_ENV=development

# Porta onde a aplicação vai rodar (8000 é padrão para desenvolvimento)
PORT=8000

# Chave secreta para sessões (MUDE ISTO EM PRODUÇÃO!)
# Gere uma chave segura em: https://randomkeygen.com/
SECRET_KEY=chave-secreta-desenvolvimento-12345

# ============================================
# 4. BANCO DE DADOS (OPCIONAL PARA DESENVOLVIMENTO)
# ============================================
# Para desenvolvimento local, você pode deixar comentado (dados em memória)
# Para produção no Render, será preenchido automaticamente
# DATABASE_URL=postgresql://usuario:senha@host:5432/nome_banco

# ============================================
# 5. REDIS (OPCIONAL)
# ============================================
# Necessário apenas se você quiser cache avançado
# REDIS_URL=redis://localhost:6379

# ============================================
# 6. AWS S3 (OPCIONAL)
# ============================================
# Necessário apenas se você quiser armazenar imagens enviadas pelos usuários
# AWS_ACCESS_KEY_ID=sua-access-key
# AWS_SECRET_ACCESS_KEY=sua-secret-key
# AWS_BUCKET_NAME=seu-bucket

# ============================================
# 7. GOOGLE CALENDAR (OPCIONAL)
# ============================================
# Necessário apenas se você quiser integração com Google Calendar para agendamentos
# GOOGLE_CALENDAR_API_KEY=sua-api-key
# GOOGLE_CALENDAR_ID=seu-calendar-id

# ============================================
# NOTAS IMPORTANTES:
# ============================================
# 1. Para desenvolvimento local: Apenas GROK_API_KEY e WHATSAPP_* são obrigatórios
# 2. Para produção no Render: Todas as variáveis obrigatórias devem ser configuradas
# 3. O banco de dados será criado automaticamente pelo Render
# 4. Nunca commite o arquivo .env no Git (já está no .gitignore)
